//Settings file for data masking


qOffset = 40; // The q index offset from which the data is outputted

lowFreqMask = true; //Boolean determining if low frequency mask is used

highFreqMask = false;  //Boolean determining if high frequency mask is used

userDefMask  = false; //Boolean determining if user defined mask is used

numGauss = 1; //The number of gaussians in the system

sigmas = [50000.0]; //The standard deviations of the user def gaussians

means = [13.0, 488.0]; //The means of the user def gaussians
